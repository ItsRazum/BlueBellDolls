export enum PhotosType {
  Photos,
  Titles,
  GenTests
}

export enum KittenClass {
    Pet = 'Pet',
    Breed = 'Breed',
    Show = 'Show'
}

export enum KittenStatus {
    Available,
    UnderObservation,
    Reserved,
    Sold
}