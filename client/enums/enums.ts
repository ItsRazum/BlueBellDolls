export enum PhotosType {
  None,
  Photos,
  Titles,
  GenTests,
}

export enum KittenClass {
  Pet,
  Breed,
  Show,
}

export enum KittenStatus {
  Available,
  UnderObservation,
  Reserved,
  Sold,
}
